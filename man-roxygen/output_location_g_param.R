#' @param location_g Character. Informs how the zonal statistics are exported.
#' Must correspond to a column of the `"location"` argument. If NULL,
#' the zonal statistics are calculated for all features of `"location"` and a
#' warning issued.
